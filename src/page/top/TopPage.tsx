import type { FC } from "react";

export const TopPage: FC = () => {
  return (
    <main className="mx-auto flex w-full max-w-2xl flex-col items-center justify-between p-3">
      <div className="w-full">これまでに作成したUIを一覧表示するサイトです。</div>
    </main>
  );
};
